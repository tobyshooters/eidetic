#!/bin/sh
sudo apt install -y libsdl2-dev  # basic window
sudo apt install -y rlwrap       # better REPL experience
sudo apt install -y scrot        # screenshots
sudo apt install -y imagemagick  # image manipulation
